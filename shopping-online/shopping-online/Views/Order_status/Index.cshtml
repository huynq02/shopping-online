@model IEnumerable<shopping_online.Context.Order_status>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Order_status_status)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order_status_status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Order_status_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Order_status_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Order_status_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
